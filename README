 |                          |         |
 __| __ `__ \  |   |\ \  /  __|  _` | __ \   __|
 |   |   |   | |   | `  <   |   (   | |   |\__ \
\__|_|  _|  _|\__,_| _/\_\ \__|\__,_|_.__/ ____/

tmux with everything removed except tabs. for running coding agents and ssh work.

no prefix key, no panes, no copy mode. chrome shortcuts:

    ctrl+T        new tab (pick claude / codex / pi / opencode / shell)
    ctrl+W        close tab
    ctrl+→ ←      next / previous tab

install:

    $ mkdir -p ~/.local/bin
    $ curl -o ~/.local/bin/tabs https://raw.githubusercontent.com/sueszli/tmux-tabs/master/tmux-tabs.sh
    $ chmod +x ~/.local/bin/tabs

usage:

    $ tabs

closing the terminal window leaves the agents running. `tabs` reattaches.
tabs close themselves when the agent exits. runs on its own tmux socket, your
~/.tmux.conf is untouched. works in the stock macOS terminal, nothing to configure.

(chrome's ctrl+tab is impossible in a terminal: it sends the same byte as tab.)
